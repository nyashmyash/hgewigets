#pragma once
/*#include <map>
#include "Widget.h"
using namespace std;

class WidgetContainer
{
private:
	bool visibl;
	std::string id;
	std::map<string,Widget> widgetsMap;
	std::map<string,WidgetContainer> widgetsContMap;
public:
	void addWidget(Widget &v, WidgetContainer *pWc);
	Widget * GetWidget(std::string id);
	WidgetContainer * GetWidgetContainer(std::string id);
	void Show(bool visibl);
	bool isVisible();
	WidgetContainer(void);
	~WidgetContainer(void);
};*/

